import React, { findDOMNode } from 'react';

var LeaguePositions = React.createClass({
  render: function() {
    return (
      <div className="LeaguePositions">
        League Positions
      </div>
    );
  }
});

export default LeaguePositions;
